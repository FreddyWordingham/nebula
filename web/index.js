import {
    Board,
    Cell,
} from "museum";
import {
    memory
} from "museum/museum_bg";



/// Main board.
// const board = Board.new(DEFAULT_WIDTH, DEFAULT_HEIGHT);
const board = Board.new(64, 64);
